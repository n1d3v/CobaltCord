<Page
    x:Class="CobaltCord.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CobaltCord"
    xmlns:data="using:CobaltCord.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding DefaultViewModel.Item, RelativeSource={RelativeSource Self}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo />
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <PaneThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="15,17,0,28">
            <TextBlock Text="CobaltCord" Margin="9,17,0,0" Style="{StaticResource ResourceKey=TitleTextBlockStyle}"  />
            <TextBlock Text="log in!" Margin="9,-7,0,-12" Style="{StaticResource HeaderTextBlockStyle}"/>
            <TextBlock Text="You need to use a Discord token, however you can use a QR code to sign into Discord." TextWrapping="Wrap" Margin="9,21,0,0" FontSize="16" Style="{StaticResource BaseTextBlockStyle}"  />
        </StackPanel>

        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="26,0">
            <StackPanel x:Name="TextBoxPanel">
                <StackPanel>
                    <TextBlock Text="Enter your Discord token (~72 characters)" Opacity="0.5" FontSize="16" Style="{StaticResource BaseTextBlockStyle}" />
                    <Grid>
                        <PasswordBox x:Name="passwordBox" Padding="10,5" BorderThickness="2" BorderBrush="White">
                            <PasswordBox.Resources>
                                <SolidColorBrush x:Key="TextControlBackground" Color="White"/>
                                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="White"/>
                                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="White"/>
                            </PasswordBox.Resources>
                        </PasswordBox>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2" VerticalAlignment="Bottom" Margin="10,0,10,7.5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Stretch" Margin="5,0" Content="go back" />
            <Button Grid.Column="1" HorizontalAlignment="Stretch" Margin="5,0" Content="next page" IsEnabled="False" />
            <Button x:Name="qrButton" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5,0" Content="scan qr" Click="qrButton_Click" />
        </Grid>
    </Grid>
</Page> 