<Page
    x:Class="CobaltCord.ConversationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CobaltCord"
    xmlns:uc="using:CobaltCord.UserControls"
    xmlns:convs="using:CobaltCord.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="65" />
            <RowDefinition Height="*" />
            <RowDefinition Height="65" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="-21,0,0,0">
                <AppBarButton x:Name="backButton" Icon="Back" Margin="0,0,-5,0" Click="backButton_Click" />
                <TextBlock x:Name="chatTextName" FontWeight="SemiBold" FontSize="24" Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Padding="0,0,0,8" />
            </StackPanel>

            <StackPanel Grid.Column="1" x:Name="buttonPanel" Orientation="Horizontal">
                <AppBarButton Icon="ContactInfo" Margin="0,0,-21,0" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <ListView ItemsSource="{Binding MessagesData}" Background="Transparent" SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,6.5"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <uc:SenderMsgItem
                                AuthorName="{Binding AuthorName}"
                                AuthorHash="{Binding AuthorHash}"
                                AuthorId="{Binding AuthorId}"
                                MessageId="{Binding MessageId}"
                                MessageTime="{Binding MessageTime}"
                                MessageText="{Binding MessageText}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="2" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AppBarButton Grid.Column="0" x:Name="attachButton" Icon="Upload" Margin="-21,0,-5,0" Click="backButton_Click" />
            <TextBox Grid.Column="1" x:Name="messageBox" Margin="0,12,0,0" Padding="10,0" Background="Transparent" BorderBrush="{ThemeResource AppBarItemForegroundThemeBrush}" AcceptsReturn="True" GotFocus="messageBox_GotFocus" LostFocus="messageBox_LostFocus" />
            <AppBarButton Grid.Column="2" Icon="Send" Margin="0,0,-21,0" />
        </Grid>
    </Grid>
</Page>